name: Testing
on: [push]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v1
      with:
        node-version: '23.x'
    - run: npm install -g yarn@3
    - run: yarn install
    - name: Running tests
      run: |
        yarn build
        yarn test
      working-directory: ./
