import React from 'react';
import PropTypes from 'prop-types';

const ButtondownAdmin = (props) => {
    return (
        <div>
            Buttondown Admin
        </div>
    )
}

ButtondownAdmin.propTypes = {
    fetch: PropTypes.func.isRequired
}

export default ButtondownAdmin;